# ============================================================
# 调试模式配置
# ============================================================
# 调试模式：无需真实手机连接，使用模拟数据
# - true: 启用调试模式（模拟设备和操作，不需要真实手机）
# - false: 正常模式（需要连接真实设备）
DEBUG_MODE=false

# 调试模式下的模拟设备信息（仅当 DEBUG_MODE=true 时生效）
DEBUG_DEVICE_NAME=模拟设备
DEBUG_SCREEN_WIDTH=1080
DEBUG_SCREEN_HEIGHT=2340

# ============================================================
# ADB 配置
# ============================================================
# Windows 需要完整路径，如：
# ADB_PATH=C:/Users/你的用户名/AppData/Local/Android/Sdk/platform-tools/adb.exe
# ADB_PATH=D:/work/Android/Sdk/platform-tools/adb.exe
# Linux/macOS 如果 adb 在 PATH 中可以直接用 adb
ADB_PATH=adb

# 默认设备地址（可选，不设置则自动检测）
# DEFAULT_DEVICE=emulator-5554
# DEFAULT_DEVICE=192.168.1.100:5555

# LLM 配置 - 使用 OpenRouter + Gemini
LLM_PROVIDER=custom
CUSTOM_LLM_API_KEY=sk-or-v1-xxxxxxxxxxxxxxxx
CUSTOM_LLM_BASE_URL=https://openrouter.ai/api/v1
CUSTOM_LLM_MODEL=google/gemini-2.5-flash-preview

# 或者使用 Claude 官方 API
# LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-xxxxxxxx
# CLAUDE_MODEL=claude-sonnet-4-20250514

# 或者使用 OpenAI
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-xxxxxxxx
# OPENAI_MODEL=gpt-4o

# 通用设置
LLM_MAX_TOKENS=1024
LLM_TEMPERATURE=0.0
LLM_TIMEOUT=60
OPERATION_DELAY=0.5

# ============================================================
# 任务分类器配置
# ============================================================
# 任务分类方式：regex（正则表达式）或 llm（使用LLM判断）
# - regex: 快速，零成本，准确率~90%（推荐）
# - llm: 更准确~95%，但有API调用成本
TASK_CLASSIFIER_MODE=regex

# === 任务分类器专用LLM配置（仅当 TASK_CLASSIFIER_MODE=llm 时生效）===
# 选项1：留空使用主LLM配置
# 选项2：指定不同的LLM提供商
# TASK_CLASSIFIER_LLM_PROVIDER=openai
# 选项3：使用自定义LLM（推荐：可用更便宜的模型如DeepSeek）
# TASK_CLASSIFIER_LLM_API_KEY=sk-xxx
# TASK_CLASSIFIER_LLM_BASE_URL=https://api.deepseek.com/v1
# TASK_CLASSIFIER_LLM_MODEL=deepseek-chat

# ============================================================
# 使用场景示例
# ============================================================
# 场景1：成本优化 - 主LLM用Claude，分类器用DeepSeek
# LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-xxx
# TASK_CLASSIFIER_MODE=llm
# TASK_CLASSIFIER_LLM_API_KEY=sk-xxx
# TASK_CLASSIFIER_LLM_BASE_URL=https://api.deepseek.com/v1
# TASK_CLASSIFIER_LLM_MODEL=deepseek-chat

# 场景2：零成本 - 使用正则表达式（推荐）
# TASK_CLASSIFIER_MODE=regex

# 场景3：统一LLM - 所有功能使用同一个LLM
# LLM_PROVIDER=openai
# TASK_CLASSIFIER_MODE=llm
# （不设置 TASK_CLASSIFIER_LLM_* 变量）
