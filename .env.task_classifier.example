# ============================================================
# 任务分类器配置示例
# ============================================================

# 方式1: 使用正则表达式判断（默认，无API开销）
TASK_CLASSIFIER_MODE=regex

# 方式2: 使用LLM判断（更准确，但有API开销）
# TASK_CLASSIFIER_MODE=llm

# ============================================================
# 任务分类器专用LLM配置（仅当 TASK_CLASSIFIER_MODE=llm 时生效）
# ============================================================

# 选项1: 使用主LLM配置（留空所有字段）
# 任务分类器会使用 LLM_PROVIDER 指定的主LLM

# 选项2: 指定不同的LLM提供商
# TASK_CLASSIFIER_LLM_PROVIDER=openai  # 使用OpenAI作为任务分类器

# 选项3: 使用自定义LLM（如DeepSeek、Moonshot等）
# TASK_CLASSIFIER_LLM_API_KEY=sk-xxx
# TASK_CLASSIFIER_LLM_BASE_URL=https://api.deepseek.com/v1
# TASK_CLASSIFIER_LLM_MODEL=deepseek-chat

# ============================================================
# 使用场景示例
# ============================================================

# 场景1: 主LLM使用Claude，任务分类使用DeepSeek（更经济）
# LLM_PROVIDER=claude
# ANTHROPIC_API_KEY=sk-ant-xxx
# CLAUDE_MODEL=claude-sonnet-4-20250514
#
# TASK_CLASSIFIER_MODE=llm
# TASK_CLASSIFIER_LLM_API_KEY=sk-xxx
# TASK_CLASSIFIER_LLM_BASE_URL=https://api.deepseek.com/v1
# TASK_CLASSIFIER_LLM_MODEL=deepseek-chat

# 场景2: 所有功能使用同一个LLM
# LLM_PROVIDER=openai
# OPENAI_API_KEY=sk-xxx
# OPENAI_BASE_URL=https://api.openai.com/v1
# OPENAI_MODEL=gpt-4o
#
# TASK_CLASSIFIER_MODE=llm
# （不设置 TASK_CLASSIFIER_LLM_* 变量，自动使用主LLM）

# 场景3: 使用正则表达式（推荐，零成本）
# TASK_CLASSIFIER_MODE=regex
# （任务分类不消耗API）
